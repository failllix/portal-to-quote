meta {
  name: Create Quote
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/quotes
  body: json
  auth: inherit
}

body:json {
  {
    "fileId": "{{file_id}}"
  }
}

vars:post-response {
  quote_id: res.body.id
}
