meta {
  name: Start File Processing
  type: http
  seq: 3
}

post {
  url: {{base_url}}/api/files/startProcessing
  body: json
  auth: inherit
}

body:json {
  {
    "id": "{{file_id}}",
    "mimeType": "text/plain",
    "originalName": "dummy-file.step",
    "sizeBytes": 18000,
    "storagePath": "stepFiles/{{file_id}}"
  }
}
